# Evidence Contract Templates

Every accepted mutation records command, run ID, source hashes, ChangeSet, staging path, snapshot ID, written paths, security findings, chunk count, post-audit path, and acceptance state. Secret values are excluded; only fingerprints may appear.
