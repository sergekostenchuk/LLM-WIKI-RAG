# Workbench Source Runtime Boundary

Private eval traces and release deliberation live outside the clean skill source. The source package contains only runtime instructions, scripts, fixtures, schemas, runbooks, and release metadata. Runtime installation must exclude private workbench evidence.
