# Tool Verification

Required:

- Python 3.11+;
- Python stdlib modules `sqlite3`, `hashlib`, `json`, `pathlib`.

Conditional:

- `pdftotext` for PDF input.

Optional/future:

- Git for snapshots in Beta 0.2;
- production embedding provider;
- production vector store.

Dependency states: `verified`, `assumed`, `missing`, `fallback`, `manual_approval_required`, `hard_blocked`. Never auto-install unknown or unpinned packages.
